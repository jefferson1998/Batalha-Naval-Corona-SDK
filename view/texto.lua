local texto = {display.newText("mensagem", 150, 30, native.systemFontBold, 40):setFillColor(1,0,1)}
local id = 1


function texto:atualizaNavioInserido()
	-- body
end

-- function texto:newTexto(nome, mensagem, largura, altura, tamanho)

	
-- end

