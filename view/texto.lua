local texto = {display.newText("mensagem", 150, 30, native.systemFontBold, 40):setFillColor(1,0,1)}
local id = 1
local navios = require "model.Navio"

-- function texto:atualizaInsercaoDeNavios()
-- 	table.insert(texto, {id = nome, texto = display.newText(mensagem, x, y, native.systemFontBold, tamanhoFont):setFillColor(cor[1], cor[2], cor[3])})
-- end

-- function texto:newTexto(nome, mensagem, largura, altura, tamanho)


-- end

